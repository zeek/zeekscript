function some_handler(c: connection)
    {
    if ( foo )
        {
        if ( bar )
            {
            if ( bletch )
                other_function_abc("Some kind of status message"); #@ NOT-TESTED
            }
        }
    }