function foo()
	{
	if ( some_condition() )
		{
		local published =
			Some::long_func_name(proxy_pool, cat(c$id$orig_h, fqdn),
			                     SomeModule::some_handler,
			                     c$id$orig_h, c$uid, fqdn,
			                     conn$orig_bytes);
		}
	}
